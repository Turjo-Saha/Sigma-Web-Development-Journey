let a = Math.round(Math.random() * 100) ;
let b = Math.round(Math.random() * 100) ;
let c = Math.round(Math.random() * 100) ;

let Adjective = {
    0: "Carzy",
    1: "Amazing",
    2: "Fire"
}
let Shop = {
    0: "Engine",
    1: "Food",
    2: "Garment"
}
let word = {
    0: "Bros",
    1: "Limited",
    2: "Hub"
}

console.log(Adjective[a], Shop[b], word[c]);

